<div class="panel">
  <div class="title">
    <h3>
      <%= link_to "Raids", raids_path %> /
      <%= link_to @raid, raid_path(@raid) %> /
      New Boss
    </h3>
  </div>

  <%= render "bosses/form", boss: @boss %>
</div>
