<div class="panel">
  <h2>New boss</h2>

  <%= form_for @boss, url: { :action => "create" } do |f| %>
    <div>
      <%= f.label :name %><br />
      <%= f.text_field :name, :autofocus => true %>
    </div>

    <div>
      <%= f.label :content %><br />
      <%= f.text_area :content %>
    </div>

    <div>
      <%= f.submit "Create" %>
    </div>
  <% end %>
</div>
