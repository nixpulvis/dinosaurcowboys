<%= simple_form_for forum do |f| %>
  <%= f.input :name %>
  <%= f.association :readable_ranks, as: :check_boxes %>
  <%= f.association :writable_ranks, as: :check_boxes %>

  <%= f.button :submit %>
<% end %>
