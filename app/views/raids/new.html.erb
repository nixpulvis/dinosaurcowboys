<div class="panel">
  <h2>New Raid</h2>

  <%= simple_form_for @raid, url: { action: "create" } do |f| %>
    <%= f.input :name %>
    <%= f.input :tier %>
    <%= f.input :content %>

    <%= f.button :submit %>
  <% end %>
</div>
